# ===================================
# REMOTE TECH SUPPORT - Backend Config
# ===================================

# Environment
NODE_ENV=production

# Server
PORT=3000

# Database (Neon PostgreSQL)
# Get this from: https://console.neon.tech/
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Alternative format (if not using DATABASE_URL)
DB_HOST=your-project.neon.tech
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password

# JWT Configuration
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-key-minimum-32-characters-change-this
JWT_EXPIRES_IN=24h

# CORS - Frontend URL
# En producción será tu dominio de Vercel
CORS_ORIGIN=https://your-app.vercel.app

# Email Configuration (Outlook/Office365)
SMTP_HOST=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_USER=your-email@domain.com
SMTP_PASSWORD=your-email-password
SMTP_FROM=your-email@domain.com

# File Uploads
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# Logging
LOG_LEVEL=info

# ===================================
# DEVELOPMENT ONLY (No usar en producción)
# ===================================

# Para desarrollo local
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/remote_support
# CORS_ORIGIN=http://localhost:5173,http://localhost:5174
# JWT_SECRET=dev-secret-key-not-for-production
